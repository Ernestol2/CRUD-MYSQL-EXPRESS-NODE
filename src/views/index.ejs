<!DOCTYPE html>
<html>
<head>
  <title>PostgreSQL CRUD</title>
  <link rel="stylesheet" type="text/css" href="/main.css">
</head>
<body>
  <h1>Welcome!</h1>
  <h2>This is a simple CRUD with Nodejs & PostgreSQL.</h2>
  <p>Test this endpoints with your RESTclient of preference.</p>
  <p>You can use ThunderClient in VScode or Postman.</p>

    <code>{ "name": "anyname", "salary": 2000 }</code>
    <br>
    <code>{ id: auto-generated }</code>

  <section>
        <!-- test -->
        <h4>TEST ROUTE:</h4>
        <a href="/ping"><p><b>/ping</b></a> you will get a pong response.</p>

        <!-- GET / -->
        <h4>GET METHODS:</h4>
        <a href="/api/employees"><p><b>/api/employees</b></a> will get a list of all employees.</p>
        <a href="/api/employees/13"><p><b>/api/employees/:id</b></a> will get you a specific employee.</p>

        <!-- POST / -->
        <h4>POST METHOD:</h4>
        <p><b>/api/employees/:id</b> will create a new employee.</p>

        <!-- delete / -->
        <h4>DELETE METHOD:</h4>
        <p><b>/api/employees/:id</b> will delete a employee.</p>

        <!-- patch -->
        <h4>PATCH METHOD:</h4>
        <p><b>/api/employees/:id</b> will update a employee.</p>
  </section>
  
  
</body>
</html>